#!/usr/bin/env node

// This script is called by the build process
// It's a v0.app utility to mark files as generated
// We can safely skip it for local development

console.log('✓ Build metadata injection skipped (local development mode)')
